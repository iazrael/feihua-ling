{
  "buildCommand": "cd frontend && npm install && npm run build && cd ../api && npm install && npx prisma generate --schema=./schema.prisma",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ],
  "env": {
    "DATABASE_URL": "file:./prod.db"
  }
}